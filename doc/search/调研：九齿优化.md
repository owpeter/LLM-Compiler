`ntops/src/ntops/kernels`中包含了算子的内核代码。帮我调研如果利用大模型重写`application`或者`arrangement`，每个算子能够进行优化的**种类**有哪些。

比如说gemm算子的kernel`mm.py`，可以通过重写application进行进行循环展开。

目前只需要调研一下几种算子：
- rmsnorm
- linear（GEMM）
- rope
- causalSoftmax
- swiglu
- randomSample

